#!/bin/sh
# marathon-shell post-install
#
# - Refresh ld cache (Marathon ships extra .so libs under /usr/lib).
# - Reload udev so the 90-backlight.rules apply on the running system.
#
# RT scheduling: granted via /etc/security/limits.d/99-marathon-rtprio.conf
# (pam_limits) rather than per-binary file capabilities. File caps mark the
# binary as AT_SECURE, which causes glibc to drop "tainted" env vars and
# breaks Qt's QDBusConnection::sessionBus() -- DBUS_SESSION_BUS_ADDRESS comes
# from the environment, and without it the shell silently fails to register
# org.marathonos.Shell, breaking every app's IPC. See docs/RT_SCHEDULING.md.

if command -v ldconfig >/dev/null 2>&1; then
    ldconfig
fi

if command -v udevadm >/dev/null 2>&1; then
    udevadm control --reload-rules 2>/dev/null || true
    udevadm trigger 2>/dev/null || true
fi

# Refresh the AT-SPI bus so accessibility comes up on first boot.
if command -v systemctl >/dev/null 2>&1; then
    systemctl --user daemon-reload 2>/dev/null || true
fi

exit 0
